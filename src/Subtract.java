// Simple utility class that provides a subtract method.
// Kept in its own file so it can be reused from other classes.
public class Subtract {
    /**
     * Return the difference of two integers.
     */
    public static int subtract(int x, int y) {
        return x - y;
    }
}
