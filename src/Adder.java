// Simple utility class that provides an add method.
// Kept in its own file so it can be reused from other classes.
public class Adder {
    /**
     * Return the sum of two integers.
     */
    public static int add(int x, int y) {
        return x + y;
    }
}
